import React from "react";
import { Text, Center, Button } from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";

function CarCharge() {
  return (
    <SafeAreaView>
      <Center>
        <Text fontSize="36px">CarCharge Page</Text>
      </Center>
    </SafeAreaView>
  );
}

export default CarCharge;
